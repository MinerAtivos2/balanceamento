name: Fetch Market Data

on:
  schedule:
    # Executa todos os dias úteis às 23:00 UTC (20:00 BRT), após fechamento da B3
    - cron: '0 23 * * 1-5'
  workflow_dispatch:  # Permite execução manual

permissions:
  contents: write

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas
      
      - name: Fetch market data
        run: python scripts/data_fetcher.py --start 2026-03-01

      - name: Generate market summary
        run: python scripts/market_summary.py
      
      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/market_data.json data/market_summary.json data/market_summary.md
          git commit -m "Update market data and summary - $(date +'%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git push
